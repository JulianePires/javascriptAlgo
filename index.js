/**
*	To use the algorithms, import the function from a file,
*	than test using an example
*/

const {nearestGreater} = require('./nearestGreater')

const array = [1, 4, 2, 1, 7, 6]
const answer = nearestGreater(array)

console.log(answer)